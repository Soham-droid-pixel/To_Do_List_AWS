# ─────────────────────────────────────────────────────────────
# Copy this file to .env and fill in the values below.
#
#   cp .env.example .env
#
# DO NOT commit .env to git. Credentials come from the EC2 IAM
# Role – only region, table name, and port are needed here.
# ─────────────────────────────────────────────────────────────

# AWS region where your DynamoDB table lives
# Example: us-east-1 | eu-west-1 | ap-southeast-1
AWS_REGION=us-east-1

# Name of the DynamoDB table (must match the table you created)
DYNAMODB_TABLE=Tasks

# Port Express listens on (Nginx proxies 80 → this port)
PORT=5000

